import React from 'react';
import PropTypes from 'prop-types';

const Bus = () => (
  <div className="Bus" data-testid="Bus">
    Bus Component
  </div>
);

Bus.propTypes = {};

Bus.defaultProps = {};

export default Bus;
